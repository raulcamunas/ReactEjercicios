export const ItemList = ({id, name, status}) => {

    return (
        <div>
            <h2>id: {id}</h2>
            <h3>name: {name}</h3>
            <p>status: {status}</p>
        </div>
    )
}